for each slice in n_slice, we diffuse over n_token steps autoregressively.
we noise the slice from 0 to 1 over the n_token steps, such that the prediction for each step is the subsequent (slightly less noisy) step.
